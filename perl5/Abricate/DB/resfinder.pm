package Abricate::DB::resfinder;

use base Exporter;
@EXPORT_OK = qw(install);

#----------------------------------------------------------------------

sub install {
  my($self) = @_;
}
 
#----------------------------------------------------------------------

1;

